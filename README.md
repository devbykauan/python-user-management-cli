# CRUD JSON (Python)

Simple CLI CRUD application with JSON persistence.

## Features
- Create user
- List users
- Update user
- Delete user
- Saves data to a local JSON file (`users.json`)

## How to run
```bash
python app.py
